#!/bin/bash

if [[ $button = 1 ]]; then
    cyclekeyboard>/dev/null
fi

inputid="1:1:AT_Translated_Set_2_keyboard"

inputdata=$(swaymsg -t get_inputs | jq ".[] | select(.identifier==\"$inputid\")")
kbd=$(echo "$inputdata" | jq -r ".xkb_active_layout_name")
echo $LABEL $kbd
echo $LABEL $kbd
echo
